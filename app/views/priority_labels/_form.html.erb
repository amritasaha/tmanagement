<%= form_for(@priority_label) do |f| %>
  <% if @priority_label.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@priority_label.errors.count, "error") %> prohibited this priority_label from being saved:</h2>

      <ul>
      <% @priority_label.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :color %><br />
    <%= f.text_field :color %>
  </div>
  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
