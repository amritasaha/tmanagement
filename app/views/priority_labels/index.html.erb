<h1>Listing priority_labels</h1>

<table>
  <tr>
    <th>Color</th>
    <th>Title</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @priority_labels.each do |priority_label| %>
  <tr>
    <td><%= priority_label.color %></td>
    <td><%= priority_label.title %></td>
    <td><%= link_to 'Show', priority_label %></td>
    <td><%= link_to 'Edit', edit_priority_label_path(priority_label) %></td>
    <td><%= link_to 'Destroy', priority_label, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Priority label', new_priority_label_path %>
